# Basic Template for a ToDoApp that uses MySQL as backend.

### Execution Instructions:
- Run 'pip install mysql-connector-python'
- Save your MySQL password as an environment variable and use that for better security.
